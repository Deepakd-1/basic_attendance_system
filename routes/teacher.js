import { Router } from "express";
import { teacherSchema, attendaceSummary } from "./schemas/db";
import z from 'zod';


export const teacherRouter = Router();

teacherRouter.post((req, res)=>{
    
});